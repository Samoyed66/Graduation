import Vue from 'vue'
import Vuex from 'vuex'
Vue.use(Vuex)
var store = new Vuex.Store({
  state: {
    // moveBol: false
  },
  mutations: {
    /* setScroll (state, val) {
      state.homeScrollTop = val
    } */
  }
})
export default store
